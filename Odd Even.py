a= float(input('Type a number to test: '))    #Always remember the brackets, otherwise it returns an error
if (a%2)==0:
    print(a, "is an even number,") #In this, the format is not a must.
else: print(a, "is is an odd number")
print("Type another number to test") #This statement will print for everything that is above it because of its indent.
    
